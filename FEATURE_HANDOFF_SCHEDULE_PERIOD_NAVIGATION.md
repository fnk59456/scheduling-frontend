# 班表期間與連續日曆導航交接說明

> 功能頁面：`/schedules`  
> 完成日期：2026-07-30  
> 本次修改範圍：前端  
> 後端程式：未修改，由後端工程師依第 6 節接續

## 1. 問題與設計決策

原本週班表的上一週／下一週會被 `ScheduleVersion.period_start` 與
`period_end` 強制限制。使用者在 9～10 月版本到達 10 月底後，無法繼續
瀏覽 11 月，容易誤以為日曆或按鈕故障。

本次保留版本期間，因為期間仍是 AI 排班、合規檢查、派生 A、簽核與
Excel 匯出的必要作用範圍；但將「日曆瀏覽範圍」與「版本有效範圍」拆開。

## 2. 完成的前端功能

主要檔案：

- `src/pages/schedules/SchedulesPage.tsx`

已完成：

1. 上一週／下一週永遠可以操作，不再 clamp 在版本開始與結束日期。
2. 切換排班版本時，仍會先定位至該版本開始週。
3. 可見週會依日期自動拼接相同機構、分店及軌別的相鄰版本，跨版本日期
   不再被「版本外」遮住，既有班次會直接顯示。
4. 版本交界提供辨識資訊：
   - 日期欄顯示該日所屬版本名稱。
   - 交界日期加上垂直分隔線。
   - 表格上方以藍色提示列摘要本週使用的版本及各自日期範圍。
5. 編輯採「先切換、再編輯」：
   - 目前選取版本的格子可正常新增、編輯與拖曳。
   - 相鄰版本班次以唯讀狀態顯示。
   - 點擊相鄰版本班次或空格，會先切換至該版本，再開啟編輯／新增表單。
6. 班次不可直接跨版本拖曳，避免一個拖曳動作隱含搬移資料至另一版本。
7. 若日期沒有任何版本涵蓋，才顯示「尚無版本」並開啟期間處理引導：
   - 目前為 B 實際版草稿：可延長目前版本。
   - A 法規版或非草稿版本：不提供延長，只能建立新版本。
   - 可建立上一期或下一期版本。
8. 若同一天被多個同軌版本涵蓋：
   - 顯示紅色「版本重疊」警告與衝突日期。
   - 衝突日期停用新增、編輯與拖曳，不做靜默合併。
9. 延長 B 草稿時：
   - 向後延長至目標日期所在月份月底。
   - 向前延長至目標日期所在月份月初。
   - 若操作來自拖曳，延長成功後會接續完成原本的移動／Combine。
10. 新增版本會預填下一期：
   - 預設沿用目前版本長度。
   - 機構與分店沿用目前版本。
   - 版本標籤依日期自動產生，使用者仍可修改。
11. 新增版本表單提供快速期間：
   - 沿用上期長度。
   - 一個月。
   - 四週。
   - 自訂。
12. 排班新增／編輯表單的日期輸入已加上版本期間的 `min`、`max`。
13. 前端送出排班前會再次檢查日期是否位於版本期間內。

### 2.1 個人月班表

主要檔案：

- `src/pages/schedules/EmployeeMonthScheduleDialog.tsx`

個人月班表已同步採用「瀏覽範圍與版本期間分離」：

1. 上個月／下個月按鈕永遠可操作，不再限制於版本開始與結束月份。
2. 每次切換月份時，查詢畫面完整 42 格日期範圍（包含前後月補位日期），
   並納入與目前版本同機構、分店、軌別的所有相鄰版本。
3. 月曆依日期歸屬拼接班次：
   - 當月日期正常顯示。
   - 前後月補位格也會顯示相鄰版本的班次，使用較淡樣式區分。
   - 版本交界加上分隔線，班次提示會標明來源版本。
4. 當月使用多個版本時，顯示藍色版本摘要。
5. 沒有任何版本涵蓋的日期顯示「尚無版本」；有版本但沒有班次則顯示
   「未排班」。
6. 同一天被多個同軌版本涵蓋時，顯示紅色「版本衝突」而不顯示任一版本
   的班次。
7. 統計卡只計算目前瀏覽月份的班次，不把前後月補位格重複計入。

### 2.2 共用日期歸屬邏輯

主要檔案：

- `src/lib/scheduleVersionTimeline.ts`

週班表與個人月班表共用同一套規則：

1. 僅納入相同機構、相同分店、相同軌別且非 archived 的版本。
2. 以版本期間判斷每一天的所屬版本。
3. 目前選取版本涵蓋該日時優先歸屬目前版本。
4. 多版本同時涵蓋同一天時回報 conflicts，由畫面阻止操作。

## 3. 既有版本搜尋規則

使用者瀏覽或操作目前版本期間以外的日期時，前端會尋找：

- 相同機構。
- 相同分店，包括兩者皆為未指定分店。
- 與目前選取版本相同軌別（A 法規版或 B 實際版）。
- 非 archived。
- 期間涵蓋目標日期。

如果有多筆符合：

1. 優先 draft。
2. 其次 published。
3. 再其次 approved。
4. 同狀態使用最新建立者。

這只是前端在現有資料可能重疊時的防禦性選擇。後端若能建立明確的版本
重疊政策，前端可再簡化。

## 4. 新增版本預填規則

從目前版本建立下一期時：

```text
next_start = current_period_end + 1 day
next_end   = next_start + current_period_length - 1 day
```

例如：

```text
目前版本：2026-09-01 ～ 2026-10-31
預填下期：2026-11-01 ～ 2026-12-31
```

如果使用者瀏覽到更遠、且沿用長度後的新期間無法涵蓋目標日期，則改以
目標日期所在月份第一天為新版本開始，再沿用原版本長度。

## 5. 前端限制不是安全邊界

本次前端已阻止一般使用者透過畫面建立版本期間外的 Schedule，但這不是
資料完整性的最終保證。API 呼叫、其他前端、管理後台或競態操作仍可能
寫入不合法資料。

因此第 6 節的後端驗證仍是必要工作。

## 6. 後端工程師待辦

後端專案目前的關鍵位置：

```text
scheduling-api/apps/schedules/serializers.py
```

### 6.1 ScheduleSerializer.validate

目前 `ScheduleSerializer.validate()` 只檢查員工與班別是否屬於版本機構，
沒有檢查 `schedule_date` 是否位於版本期間。

建議在同一個 `validate()` 補上：

```python
schedule_date = attrs.get(
    'schedule_date',
    self.instance.schedule_date if self.instance else None,
)

if (
    schedule_version
    and schedule_date
    and not (
        schedule_version.period_start
        <= schedule_date
        <= schedule_version.period_end
    )
):
    raise serializers.ValidationError({
        'schedule_date': (
            'Schedule date must be within the schedule version period.'
        )
    })
```

必須同時覆蓋：

- POST 建立 Schedule。
- PATCH 修改 Schedule 日期。
- PATCH 將 Schedule 改到另一個 schedule_version。

### 6.2 ScheduleVersionSerializer.validate

版本期間可以 PATCH，因此還需要避免把期間縮小到無法涵蓋既有班次。

建議在 `ScheduleVersionSerializer.validate()`：

1. 驗證 `period_start <= period_end`。
2. 更新既有版本時，查詢是否存在：
   - `schedule_date < new_period_start`，或
   - `schedule_date > new_period_end`。
3. 如果存在，拒絕縮短並回傳欄位錯誤；不要靜默刪除或移動班次。

### 6.3 版本狀態限制

前端只對 `version_type == actual` 且 `status == draft` 顯示延長操作。
後端仍應強制相同規則，避免直接 PATCH：

- A 法規版不可修改期間。
- published／approved／archived 不可修改期間。
- 只有 B 實際版 draft 可以延長或調整期間。

若後端產品規則不同，請與前端同步錯誤碼與訊息。

### 6.4 版本重疊政策

目前資料模型沒有明確禁止同一機構／分店的多個 B 版本期間重疊。

建議後端團隊確認：

- 是否禁止非 archived 的 B 版本重疊。
- 是否只禁止 published／approved 重疊，而允許 draft 重疊。
- A 與 B 同期間重疊是雙軌設計的一部分，不應一律禁止。

若暫時不限制，應保留前端的衝突提示，不應直接挑一個版本靜默顯示。

## 7. 建議後端測試

至少新增：

1. 建立 Schedule，日期早於版本開始，回傳 400。
2. 建立 Schedule，日期晚於版本結束，回傳 400。
3. 建立 Schedule，日期等於開始或結束，可成功。
4. PATCH Schedule 到期間外，回傳 400。
5. PATCH Schedule 並同時更換 schedule_version，使用新版本期間驗證。
6. ScheduleVersion 的開始晚於結束，回傳 400。
7. 縮短版本後會排除既有 Schedule，回傳 400。
8. 延長 B draft 版本可成功。
9. 修改 A、approved、published 或 archived 的期間，回傳 400 或 409。

## 8. 前端驗收流程

1. 開啟 2026-09-01 ～ 2026-10-31 版本。
2. 回到 2026-08-31 ～ 2026-09-06，確認 8 月與 9 月班次同時顯示。
3. 確認日期欄顯示版本名稱、9 月 1 日有分隔線，表格上方有兩版本摘要。
4. 點擊 8 月 31 日班次或空格，確認先切換至 8 月版本，再開啟表單。
5. 嘗試將 9 月班次拖到 8 月 31 日，確認跨版本拖曳被阻止。
6. 按下一週直到進入沒有任何版本涵蓋的月份，確認箭頭不會停用，日期顯示
   「尚無版本」。
7. 點擊「尚無版本」空格：
   - B 草稿應顯示「延長目前版本」與「建立新版本」。
   - A 或非草稿應只顯示建立新版本。
8. 延長 B 草稿，確認期間延長至該月份月底並開啟排班表單。
9. 從新增版本開啟表單，確認日期、分店與自動標籤已預填。
10. 測試「沿用上期長度／一個月／四週／自訂」。
11. 嘗試在排班表單手動輸入版本期間外日期，確認瀏覽器與送出驗證均阻止。
12. 開啟任一員工個人月班表，跨越版本期間瀏覽：
    - 下個月按鈕不可被停用。
    - 相鄰版本的班次可正常顯示。
    - 只有無版本涵蓋的日期顯示「尚無版本」。
13. 建立兩個同軌且期間重疊的測試版本，確認衝突日期顯示紅色警告並停用操作。

## 9. 驗證命令

```powershell
cd scheduling-frontend
npm.cmd run build
```

完成時前端 TypeScript 與 Vite build 均已通過。
