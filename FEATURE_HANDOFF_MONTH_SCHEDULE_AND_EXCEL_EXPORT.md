# 個人月班表與 Excel 匯出交接

> 專案：AI 智慧排班系統前端
> 頁面：`/schedules`
> 更新日期：2026-08-07
> 後端影響：無新增 API、model 或 migration

## 現行功能分工

Excel 下載已拆成兩個清楚的入口：

1. 個人月班表彈窗：可切換「目前版本／跨版本拼接」，自行選擇日期範圍，下載與畫面模式一致的單一員工「個人版表」。
2. 排班管理主頁：下載目前選取版本、指定日期範圍的「整合班表」。

主頁不再提供「個人版表／整合班表」格式選擇，避免使用者從管理者入口一次下載所有員工的個人區塊。

## 個人月班表

主要檔案：

- `src/pages/schedules/EmployeeMonthScheduleDialog.tsx`
- `src/pages/schedules/SchedulesPage.tsx`
- `src/index.css`

功能：

- 從員工欄位右下角月曆 icon 展開，關閉時縮回同一 icon。
- 月曆排列為週日到週六。
- 上月／下月可連續瀏覽，不受版本資料涵蓋範圍限制。
- 開啟時預設為「目前版本」，只顯示目前選取版本的班次。
- 可切換為「跨版本拼接」，顯示相同機構、相同 A/B 軌且非 archived 的所有版本班次。
- 背景仍查詢相同機構、A/B 軌且非 archived 的其他版本，用於判斷跨版本實際時間重疊。
- 同一天多個班次分塊顯示。
- 目前版本班次若與其他版本重疊，班次右下角顯示三角警示。
- 拼接模式會在班次卡片標示來源版本，並列出本月實際包含班次的版本與重疊日期。
- 排班天數與預計工時依目前顯示模式計算；第三張卡片在目前版本顯示「合規提醒」，在拼接模式顯示「重疊提醒」。

## 個人版表下載

「下載 Excel」按鈕位於個人月班表的月份切換列。點擊後展開起始／結束日期欄位；預設為目前瀏覽月份，可改成跨月或任意區間。下載資料來源跟隨當下的「目前版本／跨版本拼接」模式。

下載定義：

- 員工：目前彈窗員工一人。
- 日期：使用者選擇的起始日至結束日，兩端皆包含。
- 目前版本模式：只查詢並輸出目前選取版本。
- 跨版本拼接模式：查詢相同機構、相同 A/B 軌、非 archived 的所有版本，輸出所選區間內屬於目前員工的班次。
- 畫面與下載使用同一資料來源模式，避免預覽無班但下載出現其他版本班次。
- 重疊：同一天多個班次全部輸出，不在下載時自行裁決。
- 工作表：只有一張，名稱為「個人版表」。
- 星期：週日到週六。
- 空班月份：仍輸出員工資訊與完整月曆，每天顯示「未排班」。
- 下載時重新按範圍查詢所有相關版本，不依賴目前月曆已載入的 42 格資料，因此切換月份或背景驗證尚未完成不會造成輸出缺漏。

檔名：

```text
{員工編號}_{員工姓名}_個人班表_{目前版本或跨版本拼接}_{起始日期}_{結束日期}.xlsx
```

非法檔名字元會轉成底線。

個人版表內容：

- 員工編號、姓名、職稱。
- 排班天數與預計工時。
- 月份標題。
- 星期、日期、班別與起訖時間。
- 未排班標記。
- 班別依模板配色。
- 橫向列印、單頁寬度、頁尾日期與頁碼。

## 整合班表下載

入口為排班管理主頁的「匯出整合班表」。

下載定義：

- 版本：只使用目前選取版本，不跨版本拼接。
- 預設日期：目前週畫面中央日期所在月份的第一日至最後一日。
- 日期仍可由使用者自行修改，起訖日皆包含。
- 員工：版本機構／分店的所有在職員工；無班員工仍保留欄位。
- 工作表：只有一張，名稱為「整合班表」。
- A 欄日期、B 欄星期、C 欄起每位員工各占一欄並向右延伸。
- 跨月時插入月份分隔列。
- 同一天多個班次在同一格分行顯示。
- 底部顯示每位員工排班天數與總工時。
- 未排班顯示「未排班」，不寫成「休」。

檔名：

```text
{版本名稱}_整合班表_{起始日期}_{結束日期}.xlsx
```

## Excel 產生器

主要檔案：`src/lib/scheduleExcelExport.ts`

入口：

```ts
createScheduleWorkbook({
  schedules,
  employees,
  dateFrom,
  dateTo,
  versionLabel,
  layout: 'personal' | 'integrated',
})
```

個人下載會傳入：

```ts
employees: [employee]
layout: 'personal'
```

`groupSchedulesByEmployee()` 會先建立指定員工群組，再加入班次。因此即使 `schedules` 是空陣列，個人版表仍會產生該員工的完整空白月曆，而不是一般的「沒有資料」提示。

同一天多個班次在寫入個人月曆前會依 `shift_template.start_time` 排序，確保跨版本資料仍按時間排列。

ExcelJS 維持 dynamic import，避免增加排班頁初始 bundle：

```ts
const { createScheduleWorkbook } = await import('@/lib/scheduleExcelExport')
```

## 資料查詢

月班表會針對目前版本與同軌相關版本，以員工與畫面完整 42 格日期範圍呼叫。畫面依模式取用目前版本或全部同軌版本；即使在目前版本模式，其他版本仍只用於警示判斷：

```ts
schedulesApi.listAll({
  version,
  employee: employee.id,
  date_from: calendarStart,
  date_to: calendarEnd,
})
```

下載時會依使用者選取的 `date_from`／`date_to` 重新查詢。若為目前版本模式只查目前版本；若為跨版本拼接模式則查詢每個同軌非封存版本並合併。這可支援跨月區間，也避免只輸出畫面當下的 42 格資料。

整合班表則重新查詢使用者指定期間：

```ts
schedulesApi.listAll({ version, date_from, date_to })
employeesApi.listAll({ is_active: true, organization, branch })
```

所有查詢使用 `listAll`，避免 DRF 分頁造成班次或員工缺漏。

## 驗證紀錄

- `npm.cmd run build`：通過。
- 本機 `/schedules` 實際畫面驗證：選取「2026 07」時，Alice 2026-07-01 只顯示目前版本的「日班」，沒有把「2026 7~8月」的「短班-早」畫進同一格。
- 同一筆日班仍保有 `跨版本重疊：2026 7~8月 · 短班-早` 的 tooltip 與右下角警示。
- 個人月班表可切換目前版本與跨版本拼接；點擊「下載 Excel」會展開日期範圍，預設為目前瀏覽月份，並可改為跨月。
- 目前版本模式的下載只查目前版本；跨版本拼接模式才完成多版本查詢與活頁簿產生。
- Alice／2026 年 6 月實機驗證：目前版本模式為 0 天／0 小時；切換拼接後為 6 天／42 小時，並顯示 `Demo Week 2026-W26`、`2026 7~8月` 兩個來源版本。
- 兩種模式的下載面板均正確顯示當前模式與 2026-06-01～2026-06-30；實際執行後皆完成產檔流程並自動關閉面板。
- 使用產品的 `createScheduleWorkbook()` 產生單人測試檔。
- 有班月份：只有「個人版表」一張工作表，只含 E0001，兩個四小時班次按時間顯示，統計為排班 1 天／8 小時。
- 空班月份：仍包含 E0001 及完整月曆，統計為排班 0 天／0 小時。
- 兩份活頁簿均以 Excel 唯讀輸出 PDF，再轉 PNG 完成視覺檢查；標題、員工列、星期、日期、班次及未排班文字沒有裁切。

## 驗收清單

1. 從員工 icon 開啟個人月班表。
2. 確認預設選中「目前版本」，切至無班月份時統計與月曆皆為空班。
3. 切換「跨版本拼接」，確認其他同軌版本的班次、來源標示與重疊提醒出現。
4. 切換月份後點擊「下載 Excel」，確認預設範圍為該月第一日至最後一日。
5. 分別在兩種模式下載，確認檔名含模式、資料內容與畫面模式一致。
6. 修改成跨月日期後下載，確認檔名包含完整起訖日。
7. 確認工作表只有「個人版表」，且只有目前員工。
8. 確認空班月份仍有完整月曆。
9. 回到主頁點擊「匯出整合班表」。
10. 確認預設日期為目前瀏覽月份，仍可修改。
11. 確認主頁沒有個人版表格式選項。
12. 確認整合班表仍為日期向下、員工向右且包含無班在職員工。

## 已知範圍

- 個人版表的跨版本拼接模式不套用簽核總表的重疊裁決；未裁決重疊會保留所有班次。
- 整合班表仍是目前選取版本的匯出，不是簽核總表最終拼接結果。
- 若未來需要匯出裁決後的最終總表，應改接 `approved-timeline` API 並套用 overlap decision。
