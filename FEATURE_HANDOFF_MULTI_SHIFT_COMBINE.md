# 多班次 Combine 與合規規則交接說明

> 功能頁面：`/schedules`  
> 完成日期：2026-07-30  
> 涵蓋範圍：人工拖曳、週班表、個人月班表、合規檢查、AI 自動排班、派生 A 班表、Excel 相容性

## 1. 功能定義

原本將班次拖到已有班次的格子時會交換兩筆班表；現在改為 Combine：

- 拖到空白格：移動班次。
- 拖到已有班次的格子：將來源班次加入目標員工與日期，不交換、不刪除目標班次。
- 同一位員工同一天可保留多個不同班別。
- 同一員工、日期與班別仍不可重複，避免觸發資料庫唯一性限制。
- Combine 後即使不合規也保留資料，讓管理者自行調整。
- 合法的多班次會在同一格內依開始時間排序，分成多個獨立色塊。
- 不合法的班次在右下角顯示三角形驚嘆號；滑鼠停留可查看原因。

若在「法規版 A」上手動移動或 Combine，該版本會自動轉成「實際版」，避免修改後仍被誤認為已通過法規驗證的版本。

## 2. 合規規則語意

### 2.1 每日最大工時

規則代碼：`max_daily_hours`

- 計算同一員工同一天所有班次的 `expected_hours` 總和。
- 超過機構設定時，當日所有班次顯示警告。
- 機構沒有設定時，預設值為 8 小時。
- ShiftRule 的 `value` 建議格式：

```json
{
  "max_hours": 8
}
```

亦相容 `hours` 或 `value` 欄位。

### 2.2 班次時間重疊

規則代碼：`overlapping_shifts`

- 同一員工、同一天的班次逐一比較實際起訖時間。
- 相接但不重疊屬於合法，例如 `08:00-12:00` 與 `12:00-16:00`。
- `08:00-12:00` 與 `10:00-14:00` 屬於重疊，保留班表並顯示警告。
- 跨午夜班次以結束日加一天計算。

### 2.3 最少休息時數

規則代碼：`min_rest_hours`

此規則與每日最大工時不同：

- 每日最大工時限制同一工作日的總工時。
- 最少休息時數比較前一個工作日最後結束的班次，與下一個工作日最早開始的班次。
- 同一天內相接的多班次不再誤判為休息不足。
- 機構沒有設定時，預設值為 11 小時。

### 2.4 規則來源優先序

後端合規、AI 排班與派生 A 共用同一套解析：

1. 系統預設值。
2. 機構啟用中的 ShiftRule。
3. API 請求內明確傳入的 constraints。

因此機構規則會實際影響合規檢查與 AI 求解，不再只是設定頁中的資料。

## 3. AI 與派生 A

OR-Tools 原本限制「每位員工每天最多一個班次」，現已改為：

- 同日可指派多個不重疊班次。
- 同日所有班次的總分鐘數不可超過 `max_daily_hours`。
- 任兩個時間重疊的班別不可同時指派給同一員工。
- `min_rest_hours` 僅套用在不同工作日之間。

例如在每日上限 8 小時時，AI 可以將 `08:00-12:00` 與 `12:00-16:00` 同時排給同一員工。

派生 A 班表也會讀取機構規則，因此 B 班表中合法的多班組合可被保留；不合法內容則由求解器依相同限制修正。

## 4. 前端呈現

主要檔案：

- `src/pages/schedules/SchedulesPage.tsx`
  - 將員工／日期索引從單筆 Schedule 改為 Schedule 陣列。
  - 實作移動與 Combine。
  - 計算即時工時超標與班次重疊警告。
  - 同格多班次分塊顯示。
- `src/pages/schedules/EmployeeMonthScheduleDialog.tsx`
  - 同一天可顯示多個班次色塊。
  - 排班天數以不重複日期計算，不會把兩個班次誤算成兩天。
- `src/pages/settings/ShiftRulesPage.tsx`
  - 新增「每日最大工時」規則名稱與樣式。
- `src/types/shift.ts`
  - `ShiftRuleType` 新增 `max_daily_hours`。

Excel 匯出原本已使用同日班次陣列，個人版表與整合班表會將同一天的多個班次全部輸出，不會只保留其中一筆。

## 5. 後端實作

主要檔案：

- `apps/compliance/engine.py`
  - 新增機構 ShiftRule 解析。
  - 新增 `overlapping_shifts`。
  - 修正 `min_rest_hours` 為跨工作日比較。
  - 每日工時改用機構 `max_daily_hours`。
- `apps/ai_engine/providers/ortools_provider.py`
  - 移除每日最多一班限制。
  - 新增每日總分鐘與班次重疊限制。
- `apps/ai_engine/views.py`
  - AI 產生班表前解析機構規則。
- `apps/schedules/views.py`
  - 派生 A 使用相同機構規則。
- `apps/shifts/models.py`
  - ShiftRule 新增 `max_daily_hours` 類型。
- `apps/shifts/migrations/0005_alter_shiftrule_rule_type.py`
  - 對應 migration。

## 6. 驗證結果

已完成：

- `python manage.py migrate`
- Django migration 已套用至目前 Docker 後端。
- 合規、軟規則、派生 A 與既有 bugfix 共 46 項後端測試通過。
- 另有測試覆蓋：
  - 兩個相接的 4 小時班次合法。
  - 重疊班次會產生違規。
  - 機構每日 10 小時規則會覆蓋預設 8 小時。
  - AI 可在同日指派兩個相接的 4 小時班次。
- 前端 `npm run build` 通過。
- Docker 前端已重新啟動並確認 `/schedules` 可正常載入。

## 7. 建議驗收流程

1. 在 ShiftRule 建立並啟用「每日最大工時」，例如 `{"max_hours": 8}`。
2. 建立上午 4 小時與下午 4 小時兩個不重疊班別。
3. 將其中一班拖到另一班所在的員工／日期格。
4. 確認兩個班次分塊顯示，且沒有警告。
5. 再 Combine 一個時間重疊的班次，確認資料仍保留且右下角出現驚嘆號。
6. 再 Combine 使當日總工時超過上限，確認所有相關班次出現超時警告。
7. 執行「合規檢查」，確認正式違規結果與即時提示一致。
8. 開啟該員工月班表，確認同日多班完整顯示。
9. 匯出個人版表與整合班表，確認同日多班均被輸出。
10. 執行 AI 排班或派生 A，確認合法的同日多班可被建立或保留。

## 8. 已知邊界

- 同一員工、同一日期、同一班別不可重複；若需要兩段相同時間但不同用途，應建立不同的班別模板。
- 人工 Combine 允許不合規資料存在；AI 自動排班與派生 A 則將重疊及每日工時視為硬限制。
- 正式合規結果需按「合規檢查」重新產生；週班表同時提供前端即時計算，避免等待檢查才看見明顯問題。
